#include <stdio.h>

int main(){

         // Constant = Fixed value that cannot be altered by the program during its execution

   const float PI = 3.14159;
   //We should simply put "const" in front

   printf("%f", PI);
   return 0;
}