#include <stdio.h>

int main(){
    //arithmetic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus - give us the remainder of the division)
    // ++ (increment)
    // -- (decrement)

    int x = 144;
    int y = 64;

    float z = x/y;   // multiplication operator example
    
    printf("%f",z);
    return 0;
}