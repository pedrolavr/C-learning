#include <stdio.h>

int main()
{
    //For LOOP = Repeats a section of code a limited amount of times (with a condition)

    //  The shape:
    //   for(index, condition, what will happen)

    for(int index = 10; index >= 1; index--)
    {
        printf("%d\n", index);
    }

    return 0;
}